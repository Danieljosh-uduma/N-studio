import { studio } from "../appNstudio/frame.js";
import config from "../studio.config.js";

// Initialize the framework with the config
studio.setConfig(config);

console.log("Studio Framer Initialized with Routing");